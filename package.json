{
  "name": "calculator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded src/sass:src/css",
    "sass-prod": "sass --no-source-map --style=compressed src/sass:src/css",
    "build": "npm run sass-prod"
  },
  "devDependencies": {
    "sass": "^1.49.8"
  }
}
